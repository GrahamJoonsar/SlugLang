# This is a slug program that computes the solutions to a quadratic equation
# (-b±√(b²-4ac))/(2a)

printf "Solving a quadratic equation"

# Getting the required variables
printf "Enter A"
readFloat a
printf "Enter B"
readFloat b
printf "Enter C"
readFloat c

# Doing the calculations
float x1 ((0-b - (((b*b) - (4*a*c)) ^ 0.5)) / (2*a))
float x2 ((0-b + (((b*b) - (4*a*c)) ^ 0.5)) / (2*a))

# Printing out the answers

printf "X1: " x1
printf "X2: " x2
