include "bin/math.slug"

# sets the variable of whatever is passed in to -99
func basicallyPointers {string ptr}
    string change "eval "
    concat change ptr " (0-99)"
    exec change
    string change "mutate "
    concat change ptr
    exec change
end

factorial 5 into queso

basicallyPointers "queso"

println queso